node_modules/
dist/
.env
*.log
.DS_Store
googlesheets.json

# Claude Code 本機設定（`.local.` 依慣例不共享）。
# 全域 ~/.config/git/ignore 已有同樣規則，這裡再寫一次是為了讓任何 clone
# 都成立，不依賴單一機器的全域設定。此檔在規則建立前就被 commit 進來，
# 而 gitignore 對已追蹤的檔案無效，所以 2026-08-14 用 git rm --cached 解除追蹤。
.claude/settings.local.json
